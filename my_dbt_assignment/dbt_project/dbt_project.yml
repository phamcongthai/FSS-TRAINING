name: 'orders_dwh'
version: '1.0.0'
config-version: 2

profile: 'orders_dwh'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  orders_dwh:
    staging:
      +materialized: view
    marts:
      +materialized: table
